name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.26'
    - name: golangci-lint
      uses: golangci/golangci-lint-action@v9
      with:
        version: latest

  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.26'
    - run: go test -v ./...

  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.26'
    - name: Run govulncheck
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck ./...
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'

  build:
    runs-on: ubuntu-latest
    needs: [lint, test, security]
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.26'
    - run: go build -o gitlab-otel-exporter cmd/main.go
    - uses: actions/upload-artifact@v4
      with:
        name: gitlab-otel-exporter
        path: gitlab-otel-exporter

  release:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    needs: [lint, test, security]
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.26'
    - name: Build binaries
      run: |
        GOOS=linux GOARCH=amd64 go build -o gitlab-otel-exporter-linux-amd64 cmd/main.go
        GOOS=darwin GOARCH=amd64 go build -o gitlab-otel-exporter-darwin-amd64 cmd/main.go
        GOOS=windows GOARCH=amd64 go build -o gitlab-otel-exporter-windows-amd64.exe cmd/main.go
    - name: Upload release assets
      uses: softprops/action-gh-release@v2
      with:
        files: |
          gitlab-otel-exporter-linux-amd64
          gitlab-otel-exporter-darwin-amd64
          gitlab-otel-exporter-windows-amd64.exe
